import styled from '@emotion/styled';

import { spacing, colors } from '@app/theme';

export const HeaderStyled = styled.header({
  height: 120,
  padding: `${spacing.medium} 0`,
  backgroundColor: colors.accent01,
  color: colors.white,
});
